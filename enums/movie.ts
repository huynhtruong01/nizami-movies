export enum Languages {
    EN = 'en',
    VI = 'vi',
}

export enum LanguagesParam {
    EN = 'en-US',
}

export enum MovieListByIdType {
    SIMILAR = 'similar',
    RECOMMENDATIONS = 'recommendations',
}

export enum MovieListType {
    UPCOMING = 'upcoming',
    TOP_RATED = 'top_rated',
    POPULAR = 'popular',
    NOW_PLAYING = 'now_playing',
}
